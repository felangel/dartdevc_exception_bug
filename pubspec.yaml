name: dartdevc_exception_bug
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^3.0.0
  bloc: ^3.0.0
  equatable: ^1.0.0

flutter:
  uses-material-design: true